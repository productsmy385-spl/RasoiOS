import { PageSkeleton } from "@/components/states/page-skeleton";

/** Catalogue skeleton: the tile grid in its final shape (frontend.md §4). */
export default function NewOrderLoading() {
  return <PageSkeleton variant="cards" />;
}
