---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
| `brand_accent_hex` | CHAR(7) | N | NULL | **Theme accent** of the public site (ADR-013 §6) | `^#[0-9A-F]{6}---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
 (DB CHECK); contrast-checked ≥4.5:1 against the chosen surface mode or rejected | Required when `theme_preset = CUSTOM` | — | — | — | Yes | `restaurant.theme_updated` |
| `phone_e164` | VARCHAR(16) | N | NULL | Contact phone | E.164 (`^\+[1-9]\d{6,14}$`) | Public only if `show_phone` | — | — | — | Yes | `restaurant.profile_updated` |
| `email` | VARCHAR(254) | N | NULL | Contact email | RFC 5322 (Zod `email()`), lowercase | Public only if `show_email` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line1` | VARCHAR(160) | N | NULL | Street address | ≤160 | Public only if `show_address` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line2` | VARCHAR(160) | N | NULL | Address continuation | ≤160 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `city` | VARCHAR(80) | N | NULL | City | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `region` | VARCHAR(80) | N | NULL | State / region | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `postal_code` | VARCHAR(16) | N | NULL | Postal code | ≤16 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `country_code` | CHAR(2) | Y | — | ISO 3166-1 alpha-2 | uppercase, known code | — | — | — | — | Yes | `restaurant.settings_updated` |
| `timezone` | VARCHAR(64) | Y | — | IANA timezone (e.g. `Asia/Kolkata`) | IANA `Area/Location` name accepted by `Intl.DateTimeFormat` (`lib/time/zone.ts#isValidTimeZone`); not `Intl.supportedValuesOf`, which omits links such as `Asia/Kolkata` and `UTC` | Drives business date (ADR-010 §5) | — | — | — | Yes | `restaurant.settings_updated` |
| `currency_code` | CHAR(3) | Y | — | ISO 4217 currency for all money of this tenant | 3 uppercase letters supported by `Intl.NumberFormat` | Immutable after the first order exists | — | — | — | Yes | `restaurant.settings_updated` |
| `show_phone` | BOOLEAN | Y | `true` | Show phone on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_email` | BOOLEAN | Y | `true` | Show email on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_address` | BOOLEAN | Y | `true` | Show address on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `website_published` | BOOLEAN | Y | `false` | Public site is live | — | `false` → `/r/{slug}` returns 404 | — | — | — | Yes | `restaurant.website_published` / `_unpublished` |
| `seo_title` | VARCHAR(60) | N | NULL | `<title>` override | ≤60 | — | — | — | — | Yes | `restaurant.website_updated` |
| `seo_description` | VARCHAR(160) | N | NULL | Meta description | ≤160 | — | — | — | — | Yes | `restaurant.website_updated` |
| `default_order_type` | order_type | Y | `DINE_IN` | Preselected type in order entry | enum | — | — | — | — | Yes | `restaurant.settings_updated` |
| `auto_print_kot` | BOOLEAN | Y | `true` | Enqueue KOT print jobs on order acceptance | — | — | — | — | — | Yes | `restaurant.settings_updated` |
| `receipt_footer` | VARCHAR(280) | N | NULL | Text printed at receipt bottom | ≤280, plain text | — | — | — | — | Yes | `restaurant.settings_updated` |
| `tagline` | VARCHAR(160) | N | NULL | Short line under the restaurant name on the public site | ≤160, plain text | — | — | — | — | Yes | `restaurant.website_updated` |
| `hero_image_url` | VARCHAR(2048) | N | NULL | Hero photograph | https only (DB CHECK) + host allow-list (Q-009 A) | — | — | — | — | Yes | `restaurant.website_updated` |
| `favicon_url` | VARCHAR(2048) | N | NULL | Site icon | https only + allow-list | — | — | — | — | Yes | `restaurant.website_updated` |
| `theme_preset` | website_theme_preset | Y | `PLATFORM` | PLATFORM / CITRUS / OCEAN / BERRY / CUSTOM | enum | CUSTOM requires the three colours (DB CHECK) | — | — | — | Yes | `restaurant.theme_updated` |
| `theme_surface_mode` | website_surface_mode | Y | `DARK` | Public site on dark or light surfaces | enum | Decides which contrast pair is checked | — | — | — | Yes | `restaurant.theme_updated` |
| `theme_primary_hex` | CHAR(7) | N | NULL | Public primary colour | `^#[0-9A-F]{6}---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
| `brand_accent_hex` | CHAR(7) | N | NULL | **Theme accent** of the public site (ADR-013 §6) | `^#[0-9A-F]{6}---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
 (DB CHECK); contrast-checked ≥4.5:1 against the chosen surface mode or rejected | Required when `theme_preset = CUSTOM` | — | — | — | Yes | `restaurant.theme_updated` |
| `phone_e164` | VARCHAR(16) | N | NULL | Contact phone | E.164 (`^\+[1-9]\d{6,14}$`) | Public only if `show_phone` | — | — | — | Yes | `restaurant.profile_updated` |
| `email` | VARCHAR(254) | N | NULL | Contact email | RFC 5322 (Zod `email()`), lowercase | Public only if `show_email` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line1` | VARCHAR(160) | N | NULL | Street address | ≤160 | Public only if `show_address` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line2` | VARCHAR(160) | N | NULL | Address continuation | ≤160 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `city` | VARCHAR(80) | N | NULL | City | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `region` | VARCHAR(80) | N | NULL | State / region | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `postal_code` | VARCHAR(16) | N | NULL | Postal code | ≤16 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `country_code` | CHAR(2) | Y | — | ISO 3166-1 alpha-2 | uppercase, known code | — | — | — | — | Yes | `restaurant.settings_updated` |
| `timezone` | VARCHAR(64) | Y | — | IANA timezone (e.g. `Asia/Kolkata`) | IANA `Area/Location` name accepted by `Intl.DateTimeFormat` (`lib/time/zone.ts#isValidTimeZone`); not `Intl.supportedValuesOf`, which omits links such as `Asia/Kolkata` and `UTC` | Drives business date (ADR-010 §5) | — | — | — | Yes | `restaurant.settings_updated` |
| `currency_code` | CHAR(3) | Y | — | ISO 4217 currency for all money of this tenant | 3 uppercase letters supported by `Intl.NumberFormat` | Immutable after the first order exists | — | — | — | Yes | `restaurant.settings_updated` |
| `show_phone` | BOOLEAN | Y | `true` | Show phone on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_email` | BOOLEAN | Y | `true` | Show email on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_address` | BOOLEAN | Y | `true` | Show address on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `website_published` | BOOLEAN | Y | `false` | Public site is live | — | `false` → `/r/{slug}` returns 404 | — | — | — | Yes | `restaurant.website_published` / `_unpublished` |
| `seo_title` | VARCHAR(60) | N | NULL | `<title>` override | ≤60 | — | — | — | — | Yes | `restaurant.website_updated` |
| `seo_description` | VARCHAR(160) | N | NULL | Meta description | ≤160 | — | — | — | — | Yes | `restaurant.website_updated` |
| `default_order_type` | order_type | Y | `DINE_IN` | Preselected type in order entry | enum | — | — | — | — | Yes | `restaurant.settings_updated` |
| `auto_print_kot` | BOOLEAN | Y | `true` | Enqueue KOT print jobs on order acceptance | — | — | — | — | — | Yes | `restaurant.settings_updated` |
| `receipt_footer` | VARCHAR(280) | N | NULL | Text printed at receipt bottom | ≤280, plain text | — | — | — | — | Yes | `restaurant.settings_updated` |
 | CUSTOM only | — | — | — | Yes | `restaurant.theme_updated` |
| `theme_secondary_hex` | CHAR(7) | N | NULL | Public secondary colour | `^#[0-9A-F]{6}---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
| `brand_accent_hex` | CHAR(7) | N | NULL | **Theme accent** of the public site (ADR-013 §6) | `^#[0-9A-F]{6}---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
 (DB CHECK); contrast-checked ≥4.5:1 against the chosen surface mode or rejected | Required when `theme_preset = CUSTOM` | — | — | — | Yes | `restaurant.theme_updated` |
| `phone_e164` | VARCHAR(16) | N | NULL | Contact phone | E.164 (`^\+[1-9]\d{6,14}$`) | Public only if `show_phone` | — | — | — | Yes | `restaurant.profile_updated` |
| `email` | VARCHAR(254) | N | NULL | Contact email | RFC 5322 (Zod `email()`), lowercase | Public only if `show_email` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line1` | VARCHAR(160) | N | NULL | Street address | ≤160 | Public only if `show_address` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line2` | VARCHAR(160) | N | NULL | Address continuation | ≤160 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `city` | VARCHAR(80) | N | NULL | City | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `region` | VARCHAR(80) | N | NULL | State / region | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `postal_code` | VARCHAR(16) | N | NULL | Postal code | ≤16 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `country_code` | CHAR(2) | Y | — | ISO 3166-1 alpha-2 | uppercase, known code | — | — | — | — | Yes | `restaurant.settings_updated` |
| `timezone` | VARCHAR(64) | Y | — | IANA timezone (e.g. `Asia/Kolkata`) | IANA `Area/Location` name accepted by `Intl.DateTimeFormat` (`lib/time/zone.ts#isValidTimeZone`); not `Intl.supportedValuesOf`, which omits links such as `Asia/Kolkata` and `UTC` | Drives business date (ADR-010 §5) | — | — | — | Yes | `restaurant.settings_updated` |
| `currency_code` | CHAR(3) | Y | — | ISO 4217 currency for all money of this tenant | 3 uppercase letters supported by `Intl.NumberFormat` | Immutable after the first order exists | — | — | — | Yes | `restaurant.settings_updated` |
| `show_phone` | BOOLEAN | Y | `true` | Show phone on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_email` | BOOLEAN | Y | `true` | Show email on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_address` | BOOLEAN | Y | `true` | Show address on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `website_published` | BOOLEAN | Y | `false` | Public site is live | — | `false` → `/r/{slug}` returns 404 | — | — | — | Yes | `restaurant.website_published` / `_unpublished` |
| `seo_title` | VARCHAR(60) | N | NULL | `<title>` override | ≤60 | — | — | — | — | Yes | `restaurant.website_updated` |
| `seo_description` | VARCHAR(160) | N | NULL | Meta description | ≤160 | — | — | — | — | Yes | `restaurant.website_updated` |
| `default_order_type` | order_type | Y | `DINE_IN` | Preselected type in order entry | enum | — | — | — | — | Yes | `restaurant.settings_updated` |
| `auto_print_kot` | BOOLEAN | Y | `true` | Enqueue KOT print jobs on order acceptance | — | — | — | — | — | Yes | `restaurant.settings_updated` |
| `receipt_footer` | VARCHAR(280) | N | NULL | Text printed at receipt bottom | ≤280, plain text | — | — | — | — | Yes | `restaurant.settings_updated` |
 | CUSTOM only | — | — | — | Yes | `restaurant.theme_updated` |
| `gradient_from_hex` / `gradient_to_hex` | CHAR(7) | N | NULL | Hero gradient ends | `^#[0-9A-F]{6}---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
| `brand_accent_hex` | CHAR(7) | N | NULL | **Theme accent** of the public site (ADR-013 §6) | `^#[0-9A-F]{6}---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
 (DB CHECK); contrast-checked ≥4.5:1 against the chosen surface mode or rejected | Required when `theme_preset = CUSTOM` | — | — | — | Yes | `restaurant.theme_updated` |
| `phone_e164` | VARCHAR(16) | N | NULL | Contact phone | E.164 (`^\+[1-9]\d{6,14}$`) | Public only if `show_phone` | — | — | — | Yes | `restaurant.profile_updated` |
| `email` | VARCHAR(254) | N | NULL | Contact email | RFC 5322 (Zod `email()`), lowercase | Public only if `show_email` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line1` | VARCHAR(160) | N | NULL | Street address | ≤160 | Public only if `show_address` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line2` | VARCHAR(160) | N | NULL | Address continuation | ≤160 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `city` | VARCHAR(80) | N | NULL | City | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `region` | VARCHAR(80) | N | NULL | State / region | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `postal_code` | VARCHAR(16) | N | NULL | Postal code | ≤16 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `country_code` | CHAR(2) | Y | — | ISO 3166-1 alpha-2 | uppercase, known code | — | — | — | — | Yes | `restaurant.settings_updated` |
| `timezone` | VARCHAR(64) | Y | — | IANA timezone (e.g. `Asia/Kolkata`) | IANA `Area/Location` name accepted by `Intl.DateTimeFormat` (`lib/time/zone.ts#isValidTimeZone`); not `Intl.supportedValuesOf`, which omits links such as `Asia/Kolkata` and `UTC` | Drives business date (ADR-010 §5) | — | — | — | Yes | `restaurant.settings_updated` |
| `currency_code` | CHAR(3) | Y | — | ISO 4217 currency for all money of this tenant | 3 uppercase letters supported by `Intl.NumberFormat` | Immutable after the first order exists | — | — | — | Yes | `restaurant.settings_updated` |
| `show_phone` | BOOLEAN | Y | `true` | Show phone on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_email` | BOOLEAN | Y | `true` | Show email on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_address` | BOOLEAN | Y | `true` | Show address on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `website_published` | BOOLEAN | Y | `false` | Public site is live | — | `false` → `/r/{slug}` returns 404 | — | — | — | Yes | `restaurant.website_published` / `_unpublished` |
| `seo_title` | VARCHAR(60) | N | NULL | `<title>` override | ≤60 | — | — | — | — | Yes | `restaurant.website_updated` |
| `seo_description` | VARCHAR(160) | N | NULL | Meta description | ≤160 | — | — | — | — | Yes | `restaurant.website_updated` |
| `default_order_type` | order_type | Y | `DINE_IN` | Preselected type in order entry | enum | — | — | — | — | Yes | `restaurant.settings_updated` |
| `auto_print_kot` | BOOLEAN | Y | `true` | Enqueue KOT print jobs on order acceptance | — | — | — | — | — | Yes | `restaurant.settings_updated` |
| `receipt_footer` | VARCHAR(280) | N | NULL | Text printed at receipt bottom | ≤280, plain text | — | — | — | — | Yes | `restaurant.settings_updated` |
 | Both or neither (service rule) | — | — | — | Yes | `restaurant.theme_updated` |
| `instagram_url`, `facebook_url`, `maps_url` | VARCHAR(2048) | N | NULL | Social and map links | https only (DB CHECK) | Rendered as links on the public site | — | — | — | Yes | `restaurant.website_updated` |
| `whatsapp_e164` | VARCHAR(16) | N | NULL | WhatsApp contact | `^\+[1-9][0-9]{7,14}---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
| `brand_accent_hex` | CHAR(7) | N | NULL | **Theme accent** of the public site (ADR-013 §6) | `^#[0-9A-F]{6}---
title: "SLICE-01 Data Model — Data Dictionary, Constraints and Invariants"
document_type: "DATA_MODEL"
project: "Restaurant SaaS Platform"
project_owner: "Gopala Krishna"
slice: "SLICE-01"
status: "PROPOSED"
version: "2.0"
created: "2026-09-15"
last_updated: "2026-09-15"
owner: "Database Engineer (UNASSIGNED) — approval: Gopala Krishna"
planned_start: "Not scheduled — execution-order plan"
planned_finish: "Not scheduled — execution-order plan"
dependencies: ["RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
related_documents: ["erd.md", "tenant-isolation.md", "api.md", "../../database/database.md", "../../database/migration-strategy.md"]
related_decisions: ["RASOIOS-ADR-002", "RASOIOS-ADR-003", "RASOIOS-ADR-006", "RASOIOS-ADR-007", "RASOIOS-ADR-008", "RASOIOS-ADR-010", "RASOIOS-ADR-011"]
---

# SLICE-01 Data Model

This document lists the exact fields and constraints of the target PostgreSQL schema. It is the
source for `prisma/schema.prisma` and migration `0001_init` (task S1-P02-T002/T003). Relationships
and diagrams are in `erd.md`.

Status of every entity: **[proposed]** until migration `0001_init` exists. They are derived from the brief, CLAUDE.md, and ADR-006…011
(approved 2026-09-15, gate S1-P01-T010). Entities that exist in the baseline schema are marked *(baseline)*. Anything that
depends on an unresolved open question says so.

---

## 1. Conventions

### 1.1 Naming — no contradictions between layers

| Layer | Convention | Example |
|---|---|---|
| Conceptual entity (docs) | UPPER_SNAKE singular | `ORDER_ITEM` |
| PostgreSQL table | snake_case plural (`@@map`) | `order_items` |
| PostgreSQL column | snake_case (`@map`) | `tenant_id` |
| Prisma model | PascalCase singular | `OrderItem` |
| Prisma / TypeScript field | camelCase | `tenantId` |

`tenant_id` (column), `tenantId` (TypeScript) and "tenant" (concept) are the same attribute.
This convention is already used in the baseline (`prisma/schema.prisma` uses `@map("tenant_id")`) [fact].

### 1.2 Types

| Kind | PostgreSQL type | Prisma | Rule |
|---|---|---|---|
| Identifier | `UUID` | `String @db.Uuid @default(dbgenerated("gen_random_uuid()"))` | Generated by PostgreSQL; never sequential |
| Money | `NUMERIC(12,2)` | `Decimal @db.Decimal(12,2)` | CLAUDE.md rule 4; `Prisma.Decimal` arithmetic only (ADR-010) |
| Rate (percent) | `NUMERIC(5,2)` | `Decimal @db.Decimal(5,2)` | 0.00–100.00 |
| Timestamp | `TIMESTAMPTZ(6)` | `DateTime @db.Timestamptz(6)` | Stored UTC; displayed in `RESTAURANT.timezone` |
| Business date | `DATE` | `DateTime @db.Date` | Calendar date in restaurant timezone (ADR-010 §5) |
| Short text | `VARCHAR(n)` | `String @db.VarChar(n)` | Length enforced by DB and Zod |
| Local wall-clock time | `TIME(0)` | `DateTime @db.Time(0)` | Opening hours only; minute precision, no time zone (interpreted in `RESTAURANT.timezone`) |
| Structured document | `JSONB` | `Json` | Only for non-monetary, non-relational payloads (print render doc, redacted audit state) |

### 1.3 Standard columns

| Column | Type | Req | Default | Meaning | Validation | Security | Index | Unique | FK | Tenant scoped | Audit impact |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | Primary key | UUID v4 format on input | IDs are not secrets; access is by scope, not obscurity | PK | PK | — | — | Recorded as `resource_id` |
| `tenant_id` | UUID | Y (tenant-owned) | — | Owning tenant | Never read from client input | Set only from `TenantContext`/`AgentContext` (ADR-006/008) | Leading column of every tenant index | Part of `(tenant_id, id)` | `tenants.id` `ON DELETE RESTRICT` | Yes | Recorded as `audit_logs.tenant_id` |
| `created_at` | TIMESTAMPTZ | Y | `now()` | Creation instant (UTC) | Server-set | — | Where listed | — | — | — | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()`, `@updatedAt` | Last change instant; polling cursor (ADR-009) | Server-set | — | `(tenant_id, updated_at)` where polled | — | — | — | — |
| `archived_at` | TIMESTAMPTZ | N | NULL | Soft-archive instant; archived rows are hidden from operational lists and public site but kept for history | Server-set | Archive, not delete, for anything an order may reference | Partial indexes `WHERE archived_at IS NULL` | — | — | — | `*.archived` event |

### 1.4 Deletion policy

- **Never hard-deleted:** tenants, users, memberships, menu categories/items/variants/add-ons, customers,
  orders and all order children, KOTs, transactions, print jobs, audit logs. They use status or `archived_at`.
- **Hard-deletable:** `DAILY_MENU` in `DRAFT` (cascades to its items), `RESTAURANT_HOURS` rows (replaced as a set),
  `RATE_LIMIT_BUCKET` rows (expiry), `TENANT_COUNTER` never.
- All tenant FKs are `ON DELETE RESTRICT`.

### 1.5 Tenant integrity (ADR-008)

Every table marked *Tenant scoped = Yes* declares `UNIQUE (tenant_id, id)` (exempt: `audit_logs`, whose `tenant_id` is nullable and which nothing references; `tenant_counters`, keyed by `(tenant_id, counter_type, business_date)`). Every child references its parent with a
composite FK `(tenant_id, parent_id) → parent(tenant_id, id)`, so a row cannot reference another tenant's parent.

### 1.6 Enums

| Enum | Values | Notes |
|---|---|---|
| `tenant_status` | `ACTIVE`, `SUSPENDED` | baseline |
| `user_status` | `ACTIVE`, `INACTIVE`, `SUSPENDED` | baseline |
| `platform_role` | `NONE`, `SUPER_ADMIN` | ADR-006 |
| `tenant_role` | `TENANT_ADMIN`, `MANAGER`, `CASHIER`, `KITCHEN`, `WAITER` | ADR-006 (SUPER_ADMIN moved to `platform_role`) |
| `membership_status` | `INVITED`, `ACTIVE`, `INACTIVE` | ADR-006 |
| `dietary_type` | `VEG`, `NON_VEG`, `EGG` | Q-022 answered A (2026-09-15) |
| `daily_menu_status` | `DRAFT`, `PUBLISHED`, `UNPUBLISHED` | |
| `order_type` | `DINE_IN`, `TAKEAWAY`, `DELIVERY` | baseline; DELIVERY carries no logistics (out of scope) |
| `order_channel` | `STAFF`, `PUBLIC_WEB` | `PUBLIC_WEB` only if Q-001 approves public ordering |
| `order_status` | `NEW`, `ACCEPTED`, `PREPARING`, `READY`, `COMPLETED`, `CANCELLED`, `REFUNDED` | brief §26 |
| `order_priority` | `NORMAL`, `HIGH` | brief §29 "priority" |
| `payment_status` | `UNPAID`, `PARTIALLY_PAID`, `PAID`, `PARTIALLY_REFUNDED`, `REFUNDED` | ADR-010 §8 |
| `kot_status` | `QUEUED`, `PREPARING`, `READY`, `SERVED`, `CANCELLED` | baseline + CANCELLED |
| `transaction_type` | `PAYMENT`, `REFUND` | ADR-010 §8 |
| `payment_method` | `CASH`, `CARD`, `UPI` | baseline `ONLINE` removed until a gateway is approved (Q-015) |
| `transaction_status` | `SUCCESS`, `VOIDED` | ledger rows are recorded, not authorised by a gateway |
| `printer_purpose` | `KOT`, `RECEIPT`, `KOT_AND_RECEIPT` | |
| `printer_connection` | `USB`, `LAN` | brief §30 |
| `printer_health` | `UNKNOWN`, `ONLINE`, `OFFLINE`, `ERROR` | agent-reported |
| `print_agent_status` | `PENDING_PAIRING`, `ACTIVE`, `REVOKED` | ADR-007 |
| `print_job_type` | `KOT`, `RECEIPT`, `TEST` | |
| `print_job_status` | `PENDING`, `PROCESSING`, `PRINTED`, `FAILED` | brief §30 |
| `audit_actor_type` | `USER`, `PRINT_AGENT`, `SYSTEM`, `PUBLIC`, `WEBHOOK` | |
| `social_channel` | `INSTAGRAM`, `FACEBOOK`, `WHATSAPP`, `OTHER` | manual targets only (Q-012) |
| `social_card_type` | `DAILY_MENU`, `MENU_ITEM`, `FULL_MENU` | |
| `social_post_status` | `DRAFT`, `READY`, `MARKED_POSTED`, `ARCHIVED` | no automated "PUBLISHED" until an integration is approved |
| `counter_type` | `ORDER`, `KOT` | ADR-010 §6 |
| `media_purpose` | `LOGO`, `COVER`, `MENU_ITEM` | gated by Q-009 |
| `media_status` | `PENDING_UPLOAD`, `READY`, `DELETED` | gated by Q-009 |

---

## 2. Entity dictionary

Column legend: **Req** = NOT NULL. **Idx** = participates in a non-unique index. **Uniq** = unique constraint.
**Audit** = which audit event(s) record a change to this field (`audit_logs.action`).

### E01 — TENANT *(baseline, modified)* · table `tenants` · root of tenancy

Standard columns: `id`, `created_at`, `updated_at`. Not tenant-owned (it *is* the tenant).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(120) | Y | — | Business name of the licensed restaurant tenant | trim, 2–120 | Editable by SUPER_ADMIN only | — | — | — | root | `tenant.created`, `tenant.updated` |
| `slug` | VARCHAR(48) | Y | — | Public website path segment `/r/{slug}` | `^[a-z0-9](?:[a-z0-9-]{1,46}[a-z0-9])$`; not in reserved list (`admin`, `api`, `restaurant`, `sign-in`, `sign-up`, `account`, `r`, `offline`) | Public; used only to locate public data, never to authorise staff | — | Y | — | root | `tenant.updated` (before/after) |
| `status` | tenant_status | Y | `ACTIVE` | Whether tenant may operate | enum | SUSPENDED blocks all staff access and the public site | Y `(status)` | — | — | root | `tenant.suspended`, `tenant.reactivated` |
| `suspended_at` | TIMESTAMPTZ | N | NULL | When suspended | server-set | — | — | — | — | root | `tenant.suspended` |
| `suspension_reason` | VARCHAR(500) | N | NULL | Internal reason | 10–500 when suspending | Platform-internal; never exposed to tenant users or public | — | — | — | root | `tenant.suspended` |
| `created_by_user_id` | UUID | N | NULL | SUPER_ADMIN who created the tenant | server-set | — | — | — | `users.id` RESTRICT | root | `tenant.created` |

Commercial model: TENANT carries **no** plan, tier, subscription, billing or feature-flag columns (ADR-002).

### E02 — RESTAURANT *(baseline, modified)* · table `restaurants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Cardinality 1:1 with TENANT via `UNIQUE (tenant_id)` (Q-002 answered A, 2026-09-15).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Owning tenant | — | server-derived | — | Y (1:1) | `tenants.id` RESTRICT | Yes | — |
| `name` | VARCHAR(120) | Y | — | Public display name | trim, 2–120 | Public | — | — | — | Yes | `restaurant.profile_updated` |
| `description` | VARCHAR(1000) | N | NULL | Public description | ≤1000; plain text (no HTML) | Rendered as text only (XSS) | — | — | — | Yes | `restaurant.profile_updated` |
| `logo_url` | VARCHAR(2048) | N | NULL | Logo image | `https:` only; host in `ALLOWED_IMAGE_HOSTS` or a READY `MEDIA_ASSET` URL | Never fetched server-side from user input (SSRF — T-014) | — | — | — | Yes | `restaurant.branding_updated` |
| `cover_image_url` | VARCHAR(2048) | N | NULL | Hero image on public site | as `logo_url` | as `logo_url` | — | — | — | Yes | `restaurant.branding_updated` |
 (DB CHECK); contrast-checked ≥4.5:1 against the chosen surface mode or rejected | Required when `theme_preset = CUSTOM` | — | — | — | Yes | `restaurant.theme_updated` |
| `phone_e164` | VARCHAR(16) | N | NULL | Contact phone | E.164 (`^\+[1-9]\d{6,14}$`) | Public only if `show_phone` | — | — | — | Yes | `restaurant.profile_updated` |
| `email` | VARCHAR(254) | N | NULL | Contact email | RFC 5322 (Zod `email()`), lowercase | Public only if `show_email` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line1` | VARCHAR(160) | N | NULL | Street address | ≤160 | Public only if `show_address` | — | — | — | Yes | `restaurant.profile_updated` |
| `address_line2` | VARCHAR(160) | N | NULL | Address continuation | ≤160 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `city` | VARCHAR(80) | N | NULL | City | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `region` | VARCHAR(80) | N | NULL | State / region | ≤80 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `postal_code` | VARCHAR(16) | N | NULL | Postal code | ≤16 | as above | — | — | — | Yes | `restaurant.profile_updated` |
| `country_code` | CHAR(2) | Y | — | ISO 3166-1 alpha-2 | uppercase, known code | — | — | — | — | Yes | `restaurant.settings_updated` |
| `timezone` | VARCHAR(64) | Y | — | IANA timezone (e.g. `Asia/Kolkata`) | IANA `Area/Location` name accepted by `Intl.DateTimeFormat` (`lib/time/zone.ts#isValidTimeZone`); not `Intl.supportedValuesOf`, which omits links such as `Asia/Kolkata` and `UTC` | Drives business date (ADR-010 §5) | — | — | — | Yes | `restaurant.settings_updated` |
| `currency_code` | CHAR(3) | Y | — | ISO 4217 currency for all money of this tenant | 3 uppercase letters supported by `Intl.NumberFormat` | Immutable after the first order exists | — | — | — | Yes | `restaurant.settings_updated` |
| `show_phone` | BOOLEAN | Y | `true` | Show phone on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_email` | BOOLEAN | Y | `true` | Show email on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `show_address` | BOOLEAN | Y | `true` | Show address on public site | — | Privacy control | — | — | — | Yes | `restaurant.website_updated` |
| `website_published` | BOOLEAN | Y | `false` | Public site is live | — | `false` → `/r/{slug}` returns 404 | — | — | — | Yes | `restaurant.website_published` / `_unpublished` |
| `seo_title` | VARCHAR(60) | N | NULL | `<title>` override | ≤60 | — | — | — | — | Yes | `restaurant.website_updated` |
| `seo_description` | VARCHAR(160) | N | NULL | Meta description | ≤160 | — | — | — | — | Yes | `restaurant.website_updated` |
| `default_order_type` | order_type | Y | `DINE_IN` | Preselected type in order entry | enum | — | — | — | — | Yes | `restaurant.settings_updated` |
| `auto_print_kot` | BOOLEAN | Y | `true` | Enqueue KOT print jobs on order acceptance | — | — | — | — | — | Yes | `restaurant.settings_updated` |
| `receipt_footer` | VARCHAR(280) | N | NULL | Text printed at receipt bottom | ≤280, plain text | — | — | — | — | Yes | `restaurant.settings_updated` |
 (DB CHECK) | — | — | — | — | Yes | `restaurant.website_updated` |
| `gstin` | VARCHAR(15) | N | NULL | Indian GST registration number. When set, receipts print it and split tax into CGST/SGST (ADR-010 §3, Q-004) | trim, uppercase; `^[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z][1-9A-Z]Z[0-9A-Z]$` | Printed on receipts; not part of the public website projection | — | — | — | Yes | `restaurant.settings_updated` |

### E02a — WEBSITE_SECTION *(new, ADR-013 §6)* · table `website_sections`

Which sections a restaurant's public site shows, in what order, with the copy the restaurant controls. The section
**keys are a fixed enum** — a tenant configures content, never layout or markup.

| Column | Type | Req | Default | Meaning | Validation | Rule | Unique | Index | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `id` | UUID | Y | `gen_random_uuid()` | PK | — | — | `(tenant_id, id)` | — | — | Yes | — |
| `tenant_id` | UUID | Y | — | Owner tenant | — | From context only | — | see below | `tenants(id)` RESTRICT | Yes | — |
| `restaurant_id` | UUID | Y | — | Parent restaurant | — | — | — | — | `(tenant_id, restaurant_id) → restaurants(tenant_id, id)` CASCADE | Yes | — |
| `key` | website_section_key | Y | — | HERO, ABOUT, FEATURED_MENU, CATEGORIES, POPULAR_ITEMS, INFO, HOURS, GALLERY, LOCATION, CONTACT, CTA | enum | One row per key per restaurant | `(tenant_id, restaurant_id, key)` | — | — | Yes | `restaurant.website_updated` |
| `enabled` | BOOLEAN | Y | `true` | Section is rendered | — | HERO cannot be disabled (service rule) | — | — | — | Yes | `restaurant.website_updated` |
| `sort_order` | SMALLINT | Y | `0` | Display order | 0–999 (DB CHECK) | — | — | `(tenant_id, restaurant_id, sort_order)` | — | Yes | `restaurant.website_updated` |
| `headline` | VARCHAR(120) | N | NULL | Section heading | ≤120, plain text | — | — | — | — | Yes | `restaurant.website_updated` |
| `body` | VARCHAR(1000) | N | NULL | Section copy | ≤1000, plain text (no HTML) | Rendered as text, never as markup (SC-VAL-03) | — | — | — | Yes | `restaurant.website_updated` |
| `image_url` | VARCHAR(2048) | N | NULL | Section image | https only (DB CHECK) + allow-list | — | — | — | — | Yes | `restaurant.website_updated` |
| `cta_label` / `cta_href` | VARCHAR(40) / VARCHAR(2048) | N | NULL | Optional call to action | label ≤40; href https or internal `/…` (DB CHECK) | Both or neither (DB CHECK) | — | — | — | Yes | `restaurant.website_updated` |
| `created_at`, `updated_at` | TIMESTAMPTZ(6) | Y | `now()` | — | — | — | — | — | — | Yes | — |

A missing row means "use the default for that section", so a new restaurant renders a complete site before anyone edits it.

### E03 — RESTAURANT_HOURS *(new)* · table `restaurant_hours`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. Replaces free-text JSON `opening_hours` (`prisma/schema.prisma:130`).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `restaurant_id` | UUID | Y | — | Restaurant | — | composite FK | — | part of U1 | `(tenant_id, restaurant_id)` → `restaurants(tenant_id, id)` CASCADE | Yes | — |
| `day_of_week` | SMALLINT | Y | — | ISO weekday 1=Mon … 7=Sun | 1–7 | — | — | part of U1 | — | Yes | `restaurant.hours_updated` (whole set before/after) |
| `sequence` | SMALLINT | Y | `1` | Shift number within day (split shifts) | 1–3 | — | — | part of U1 | — | Yes | as above |
| `is_closed` | BOOLEAN | Y | `false` | Closed all day | if true, times must be NULL | — | — | — | — | Yes | as above |
| `opens_at` | TIME(0) | N | NULL | Local opening time | required unless closed | — | — | — | — | Yes | as above |
| `closes_at` | TIME(0) | N | NULL | Local closing time; `< opens_at` means closes after midnight | required unless closed; ≠ opens_at | — | — | — | — | Yes | as above |

U1 = `UNIQUE (restaurant_id, day_of_week, sequence)`.

### E04 — KITCHEN_SECTION *(new)* · table `kitchen_sections`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`, `archived_at`. Replaces hard-coded stations (`app/restaurant/kds/page.tsx:130`).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(60) | Y | — | Display name ("Tandoor") | trim, 2–60 | — | — | — | — | Yes | `kitchen_section.created/updated` |
| `code` | VARCHAR(24) | Y | — | Stable code ("TANDOOR") | `^[A-Z0-9_]{2,24}$` | — | — | `(tenant_id, code)` | — | Yes | as above |
| `sort_order` | INTEGER | Y | `0` | Board column order | 0–9999 | — | `(tenant_id, sort_order)` | — | — | Yes | `kitchen_section.reordered` |

### E05 — USER *(baseline, modified)* · table `users` · global identity (not tenant-owned)

Standard columns: `id`, `created_at`, `updated_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `clerk_user_id` | VARCHAR(64) | N | NULL | Clerk user ID; NULL until invitation accepted | Clerk format `user_…` | Set only by session resolver/webhook | — | Y | — | No | `user.linked` |
| `email` | VARCHAR(254) | Y | — | Primary verified email, lowercase | email; normalised trim+lowercase | PII; never logged in full (logger masks) | — | Y | — | No | `user.email_synced` |
| `full_name` | VARCHAR(120) | N | NULL | Display name | ≤120 | PII | — | — | — | No | `user.profile_synced` |
| `platform_role` | platform_role | Y | `NONE` | Platform authority | enum | Changed only by seed/CLI or another SUPER_ADMIN (not exposed in SLICE-01 UI) | `(platform_role)` | — | — | No | `platform.role_changed` |
| `status` | user_status | Y | `ACTIVE` | Global account state | enum | Non-ACTIVE denies all access next request | — | — | — | No | `user.status_changed` |
| `last_sign_in_at` | TIMESTAMPTZ | N | NULL | Last successful session resolution (throttled to 1 write / 15 min) | server-set | — | — | — | — | No | — |

### E06 — USER_TENANT *(baseline, modified)* · table `user_tenants` · **authorization membership, not a subscription**

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`. USER_TENANT links a person to a tenant with one role. It has no commercial meaning (ADR-002).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `user_id` | UUID | Y | — | Member | — | — | `(user_id, status)` | `(tenant_id, user_id)` | `users.id` RESTRICT | Yes | — |
| `role` | tenant_role | Y | — | Tenant role | enum; actor may assign only roles ranked below own (TENANT_ADMIN may assign any tenant role) | Privilege-escalation control SC-RBAC-04 | `(tenant_id, role, status)` | — | — | Yes | `staff.role_changed` (before/after) |
| `status` | membership_status | Y | `INVITED` | Membership state | enum | Only ACTIVE grants access | as above | — | — | Yes | `staff.invited`, `staff.activated`, `staff.deactivated` |
| `invited_by_user_id` | UUID | N | NULL | Inviter | server-set | — | — | — | `users.id` RESTRICT | Yes | `staff.invited` |
| `invited_at` | TIMESTAMPTZ | N | NULL | Invitation time | server-set | — | — | — | — | Yes | — |
| `accepted_at` | TIMESTAMPTZ | N | NULL | First successful sign-in linked to this membership | server-set | — | — | — | — | Yes | `staff.activated` |
| `clerk_invitation_id` | VARCHAR(64) | N | NULL | Clerk invitation reference for resend/revoke | — | — | — | Y | — | Yes | — |
| `deactivated_at` | TIMESTAMPTZ | N | NULL | When access removed | server-set | — | — | — | — | Yes | `staff.deactivated` |
| `deactivated_by_user_id` | UUID | N | NULL | Who removed access | server-set | — | — | — | `users.id` RESTRICT | Yes | `staff.deactivated` |

Invariant: a tenant always has ≥1 ACTIVE TENANT_ADMIN once activated. The last active TENANT_ADMIN cannot be demoted or deactivated (SC-RBAC-05).

### E07 — MENU_CATEGORY *(baseline, modified)* · table `menu_categories`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`, `archived_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(80) | Y | — | Category name | trim, 2–80 | Public when published | — | `(tenant_id, lower(name)) WHERE archived_at IS NULL` (raw SQL) | — | Yes | `menu_category.created/updated` |
| `description` | VARCHAR(500) | N | NULL | Short description | ≤500 plain text | Public | — | — | — | Yes | `menu_category.updated` |
| `icon_key` | VARCHAR(40) | N | NULL | Icon from the curated Lucide set (`design.md` §5) | member of `MENU_ICON_KEYS` | No arbitrary SVG/HTML | — | — | — | Yes | `menu_category.updated` |
| `sort_order` | INTEGER | Y | `0` | Display order | 0–9999 | — | `(tenant_id, archived_at, sort_order)` | — | — | Yes | `menu_category.reordered` |
| `is_published` | BOOLEAN | Y | `true` | Visible on public site | — | — | `(tenant_id, is_published)` | — | — | Yes | `menu_category.published/unpublished` |

### E08 — MENU_ITEM *(baseline, modified)* · table `menu_items`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`, `archived_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `category_id` | UUID | Y | — | Category | must be same tenant, not archived | composite FK | `(tenant_id, category_id, display_order)` | — | `(tenant_id, category_id)` → `menu_categories` RESTRICT | Yes | `menu_item.updated` |
| `kitchen_section_id` | UUID | N | NULL | Where the item is prepared (KOT routing) | same tenant, not archived | composite FK | — | — | `(tenant_id, kitchen_section_id)` → `kitchen_sections` RESTRICT | Yes | `menu_item.updated` |
| `name` | VARCHAR(120) | Y | — | Dish name | trim, 2–120 | Public when published | — | — | — | Yes | `menu_item.created/updated` |
| `description` | VARCHAR(1000) | N | NULL | Dish description | ≤1000 plain text | Public | — | — | — | Yes | `menu_item.updated` |
| `image_url` | VARCHAR(2048) | N | NULL | Dish image | as `RESTAURANT.logo_url` | SSRF rule SC-VAL-04 | — | — | — | Yes | `menu_item.updated` |
| `icon_key` | VARCHAR(40) | N | NULL | Fallback icon when no image | member of `MENU_ICON_KEYS` | — | — | — | — | Yes | `menu_item.updated` |
| `base_price` | NUMERIC(12,2) | Y | — | Unit price when the item has no variants | `^\d{1,10}(\.\d{1,2})?$`; CHECK ≥ 0 | Authoritative server price (T-006) | — | — | — | Yes | `menu_item.price_changed` (before/after) |
| `tax_rate` | NUMERIC(5,2) | Y | `0.00` | Tax percent applied per line (ADR-010 §3) | CHECK 0–100 | — | — | — | — | Yes | `menu_item.price_changed` |
| `dietary_type` | dietary_type | N | NULL | Veg / non-veg / egg marker | enum (Q-022) | Public | — | — | — | Yes | `menu_item.updated` |
| `prep_time_minutes` | SMALLINT | N | NULL | Typical preparation time (kitchen timer baseline) | 0–240 | — | — | — | — | Yes | `menu_item.updated` |
| `is_available` | BOOLEAN | Y | `true` | Operational availability (e.g. sold out today) | — | Unavailable items are rejected at order creation | `(tenant_id, is_published, is_available)` | — | — | Yes | `menu_item.availability_changed` |
| `is_published` | BOOLEAN | Y | `false` | Visible on public site and orderable | — | — | as above | — | — | Yes | `menu_item.published/unpublished` |
| `display_order` | INTEGER | Y | `0` | Order within category | 0–9999 | — | see above | — | — | Yes | `menu_item.reordered` |

Removed from baseline: `variants Json`, `add_ons Json` (BA-17) → E09, E10.

### E09 — MENU_ITEM_VARIANT *(new)* · table `menu_item_variants`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`, `archived_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `menu_item_id` | UUID | Y | — | Parent item | same tenant | composite FK | `(tenant_id, menu_item_id, display_order)` | — | `(tenant_id, menu_item_id)` → `menu_items` RESTRICT | Yes | `menu_item.variants_updated` (set before/after) |
| `name` | VARCHAR(60) | Y | — | "Half", "Full", "500 ml" | trim, 1–60 | Public | — | `(menu_item_id, lower(name)) WHERE archived_at IS NULL` | — | Yes | as above |
| `price` | NUMERIC(12,2) | Y | — | Absolute unit price for this variant | CHECK ≥ 0 | Authoritative price | — | — | — | Yes | as above |
| `is_default` | BOOLEAN | Y | `false` | Preselected variant | ≤1 default per item (partial unique) | — | — | `(menu_item_id) WHERE is_default AND archived_at IS NULL` | — | Yes | as above |
| `is_available` | BOOLEAN | Y | `true` | Orderable | — | — | — | — | — | Yes | as above |
| `display_order` | INTEGER | Y | `0` | Order | 0–999 | — | — | — | — | Yes | as above |

### E10 — MENU_ITEM_ADDON *(new)* · table `menu_item_addons`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`, `archived_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `menu_item_id` | UUID | Y | — | Parent item | same tenant | composite FK | `(tenant_id, menu_item_id, display_order)` | — | `(tenant_id, menu_item_id)` → `menu_items` RESTRICT | Yes | `menu_item.addons_updated` |
| `name` | VARCHAR(60) | Y | — | "Extra cheese" | trim, 1–60 | Public | — | `(menu_item_id, lower(name)) WHERE archived_at IS NULL` | — | Yes | as above |
| `price` | NUMERIC(12,2) | Y | — | Additive price per unit | CHECK ≥ 0 | Authoritative price | — | — | — | Yes | as above |
| `is_available` | BOOLEAN | Y | `true` | Orderable | — | — | — | — | — | Yes | as above |
| `display_order` | INTEGER | Y | `0` | Order | 0–999 | — | — | — | — | Yes | as above |

### E11 — DAILY_MENU *(baseline, modified)* · table `daily_menus`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `business_date` | DATE | Y | — | Restaurant-local date the menu applies to | ISO `YYYY-MM-DD`; not earlier than today (restaurant tz) when publishing | — | — | `(tenant_id, business_date)` | — | Yes | `daily_menu.*` |
| `status` | daily_menu_status | Y | `DRAFT` | Lifecycle | transitions: DRAFT→PUBLISHED, PUBLISHED→UNPUBLISHED, UNPUBLISHED→PUBLISHED | Public site shows only PUBLISHED for today's business date | `(tenant_id, status, business_date)` | — | — | Yes | `daily_menu.published/unpublished` |
| `title` | VARCHAR(80) | N | NULL | Heading ("Today's Specials") | ≤80 | Public | — | — | — | Yes | `daily_menu.updated` |
| `note` | VARCHAR(280) | N | NULL | Public note | ≤280 plain text | Public | — | — | — | Yes | `daily_menu.updated` |
| `published_at` | TIMESTAMPTZ | N | NULL | Last publish instant | server-set | — | — | — | — | Yes | `daily_menu.published` |
| `published_by_user_id` | UUID | N | NULL | Publisher | server-set | — | — | — | `users.id` RESTRICT | Yes | `daily_menu.published` |
| `unpublished_at` | TIMESTAMPTZ | N | NULL | Last unpublish instant | server-set | — | — | — | — | Yes | `daily_menu.unpublished` |
| `copied_from_daily_menu_id` | UUID | N | NULL | Source menu when created by copy | same tenant | composite FK | — | — | `(tenant_id, copied_from_daily_menu_id)` → `daily_menus` RESTRICT (see note below) | Yes | `daily_menu.copied` |
| `created_by_user_id` | UUID | Y | — | Creator | server-set | — | — | — | `users.id` RESTRICT | Yes | `daily_menu.created` |

"Schedule": a PUBLISHED menu with a future `business_date` becomes visible automatically when that date begins in the restaurant timezone. It is evaluated at read time, so no background job is needed (REQ-DMENU-005).

**Copy-source reference (resolved 2026-09-22).** `copied_from_daily_menu_id` uses `ON DELETE RESTRICT`. `SET NULL` on the composite
key would also null the NOT NULL `tenant_id`, and Prisma cannot express PostgreSQL 15's column-list form `SET NULL (copied_from_daily_menu_id)`.
Before hard-deleting a DRAFT menu, the daily-menu service clears `copied_from_daily_menu_id` on its copies in the same transaction (S1-P11-T001).

### E12 — DAILY_MENU_ITEM *(baseline, modified)* · table `daily_menu_items`

Standard columns: `id`, `tenant_id`, `created_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `daily_menu_id` | UUID | Y | — | Parent daily menu | same tenant | composite FK | `(tenant_id, daily_menu_id, display_order)` | `(daily_menu_id, menu_item_id)` | `(tenant_id, daily_menu_id)` → `daily_menus` CASCADE | Yes | `daily_menu.items_updated` (set before/after) |
| `menu_item_id` | UUID | Y | — | Included item | same tenant; not archived; published to publish the menu | composite FK | — | as above | `(tenant_id, menu_item_id)` → `menu_items` RESTRICT | Yes | as above |
| `display_order` | INTEGER | Y | `0` | Order within daily menu | 0–999 | — | — | — | — | Yes | as above |

### E13 — CUSTOMER *(baseline, modified)* · table `customers`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`, `archived_at`. Data minimisation (brief §27): only name, phone, email, notes.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `full_name` | VARCHAR(120) | Y | — | Customer name | trim, 1–120 | PII; hidden from KITCHEN role | `(tenant_id, full_name)` | — | — | Yes | `customer.created/updated` (PII masked in audit) |
| `phone_e164` | VARCHAR(16) | N | NULL | Lookup key | E.164 | PII; masked in logs (`+91******3366`) | — | `(tenant_id, phone_e164) WHERE phone_e164 IS NOT NULL` | — | Yes | as above |
| `email` | VARCHAR(254) | N | NULL | Receipt/contact email | email, lowercase | PII | `(tenant_id, email)` | — | — | Yes | as above |
| `notes` | VARCHAR(500) | N | NULL | Staff notes (preferences, allergies) | ≤500 plain text | Staff-only; never public, never on KOT | — | — | — | Yes | `customer.updated` |
| `created_by_user_id` | UUID | N | NULL | Staff creator (NULL for public web if Q-001) | server-set | — | — | — | `users.id` RESTRICT | Yes | `customer.created` |
| `anonymized_at` | TIMESTAMPTZ | N | NULL | Erasure performed: name → "Anonymised customer", phone/email/notes → NULL | server-set; irreversible | Supports erasure requests (Q-020) | — | — | — | Yes | `customer.anonymized` |

Rule: public or staff order submission never overwrites an existing customer's stored name or email (fixes BA-19). Updates go only through `SA-CUS-02`.

### E14 — ORDER *(baseline, modified)* · table `orders`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `order_number` | VARCHAR(20) | Y | — | Human number `YYYYMMDD-NNNN` | server-generated (ADR-010 §6) | — | — | `(tenant_id, order_number)` | — | Yes | `order.created` |
| `business_date` | DATE | Y | — | Restaurant-local date of creation | server-computed | — | `(tenant_id, business_date)` | — | — | Yes | — |
| `order_type` | order_type | Y | — (service pre-fills `RESTAURANT.default_order_type`) | Dine-in / takeaway / delivery | enum | — | — | — | — | Yes | `order.created` |
| `channel` | order_channel | Y | `STAFF` | Origin | server-set | — | — | — | — | Yes | `order.created` |
| `status` | order_status | Y | `NEW` | Lifecycle (`prd.md` BR-ORD-02) | state machine | Transitions permission-gated (security.md §3.4) | `(tenant_id, status, updated_at)` | — | — | Yes | `order.status_changed` (from/to) |
| `priority` | order_priority | Y | `NORMAL` | Kitchen priority | enum; HIGH settable by TENANT_ADMIN, MANAGER, CASHIER | — | — | — | — | Yes | `order.priority_changed` |
| `payment_status` | payment_status | Y | `UNPAID` | Derived from ledger in same transaction | server-computed only | Never client-set | `(tenant_id, payment_status)` | — | — | Yes | `order.payment_status_changed` |
| `table_label` | VARCHAR(20) | N | NULL | Table identifier for dine-in | ≤20 `^[A-Za-z0-9 -]+$` | — | — | — | — | Yes | `order.created` |
| `customer_id` | UUID | N | NULL | Linked customer | same tenant | composite FK | `(tenant_id, customer_id, created_at)` | — | `(tenant_id, customer_id)` → `customers` RESTRICT | Yes | `order.customer_linked` |
| `notes` | VARCHAR(500) | N | NULL | Order-level notes (printed on KOT) | ≤500 plain text | No PII expected; shown to kitchen | — | — | — | Yes | `order.created` |
| `subtotal_amount` | NUMERIC(12,2) | Y | — | Σ line_subtotal | server-computed; CHECK ≥ 0 | Client value rejected | — | — | — | Yes | `order.created` |
| `tax_amount` | NUMERIC(12,2) | Y | — | Σ line_tax | server-computed; CHECK ≥ 0 | as above | — | — | — | Yes | `order.created` |
| `discount_amount` | NUMERIC(12,2) | Y | `0.00` | Always 0 until a discount feature is approved (Q-006) | CHECK = 0 in SLICE-01 | as above | — | — | — | Yes | — |
| `total_amount` | NUMERIC(12,2) | Y | — | subtotal + tax − discount | CHECK `total_amount = subtotal_amount + tax_amount - discount_amount` | as above | — | — | — | Yes | `order.created`, `order.items_added` |
| `paid_amount` | NUMERIC(12,2) | Y | `0.00` | Σ successful PAYMENT amounts | server-computed | — | — | — | — | Yes | `payment.recorded` |
| `refunded_amount` | NUMERIC(12,2) | Y | `0.00` | Σ successful REFUND amounts | CHECK ≤ paid_amount | — | — | — | — | Yes | `refund.created` |
| `currency_code` | CHAR(3) | Y | — | Snapshot of restaurant currency | server-set | — | — | — | — | Yes | — |
| `idempotency_key` | UUID | Y | — | Client-generated per submit attempt | UUID | Replay-safe create (T-006) | — | `(tenant_id, idempotency_key)` | — | Yes | — |
| `created_by_user_id` | UUID | N | NULL | Staff creator (NULL for PUBLIC_WEB) | server-set | — | — | — | `users.id` RESTRICT | Yes | `order.created` |
| `accepted_at` / `preparing_at` / `ready_at` / `completed_at` / `refunded_at` | TIMESTAMPTZ | N | NULL | Transition instants | server-set | — | — | — | — | Yes | `order.status_changed` |
| `cancelled_at` | TIMESTAMPTZ | N | NULL | Cancellation instant | server-set | — | — | — | — | Yes | `order.cancelled` |
| `cancelled_by_user_id` | UUID | N | NULL | Who cancelled | server-set | — | — | — | `users.id` RESTRICT | Yes | `order.cancelled` |
| `cancel_reason` | VARCHAR(280) | N | NULL | Why | required (5–280) when cancelling | — | — | — | — | Yes | `order.cancelled` |
| `version` | INTEGER | Y | `0` | Optimistic concurrency counter | incremented on every update; updates use `WHERE version = $expected` | Prevents lost updates between tablets | — | — | — | Yes | — |

Additional index: `(tenant_id, updated_at)` (ADR-009 cursor).

### E15 — ORDER_ITEM *(baseline, modified)* · table `order_items` · immutable after insert

Standard columns: `id`, `tenant_id`, `created_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `order_id` | UUID | Y | — | Parent order | same tenant | composite FK | `(tenant_id, order_id)` | — | `(tenant_id, order_id)` → `orders` RESTRICT | Yes | `order.created` / `order.items_added` (lines in after_state) |
| `menu_item_id` | UUID | Y | — | Source item (history link only) | same tenant; published, available, not archived at order time | Never used to recompute price | `(tenant_id, menu_item_id)` (menu performance report) | — | `(tenant_id, menu_item_id)` → `menu_items` RESTRICT | Yes | as above |
| `variant_id` | UUID | N | NULL | Chosen variant | required iff item has active variants; must belong to `menu_item_id` | — | — | — | `(tenant_id, variant_id)` → `menu_item_variants` RESTRICT | Yes | as above |
| `kitchen_section_id` | UUID | N | NULL | Section snapshot for KOT routing | copied from item at order time | — | — | — | `(tenant_id, kitchen_section_id)` → `kitchen_sections` RESTRICT | Yes | — |
| `kot_round` | SMALLINT | Y | `1` | Which KOT round the line belongs to (Q-003) | ≥1 | — | — | — | — | Yes | — |
| `item_name_snapshot` | VARCHAR(120) | Y | — | Item name at order time | copied | — | — | — | — | Yes | — |
| `variant_name_snapshot` | VARCHAR(60) | N | NULL | Variant name at order time | copied | — | — | — | — | Yes | — |
| `unit_price_snapshot` | NUMERIC(12,2) | Y | — | Base or variant price at order time | copied; ≥0 | Authoritative | — | — | — | Yes | — |
| `addons_total_snapshot` | NUMERIC(12,2) | Y | `0.00` | Σ add-on price per unit | computed | — | — | — | — | Yes | — |
| `tax_rate_snapshot` | NUMERIC(5,2) | Y | — | Tax rate at order time | copied | — | — | — | — | Yes | — |
| `quantity` | SMALLINT | Y | — | Units | 1–99 | — | — | — | — | Yes | — |
| `line_subtotal` | NUMERIC(12,2) | Y | — | (unit + add-ons) × qty | computed; CHECK `line_subtotal = (unit_price_snapshot + addons_total_snapshot) * quantity` | — | — | — | — | Yes | — |
| `line_tax` | NUMERIC(12,2) | Y | — | ROUND_HALF_UP(subtotal × rate / 100, 2) | computed | — | — | — | — | Yes | — |
| `line_total` | NUMERIC(12,2) | Y | — | subtotal + tax | CHECK `line_total = line_subtotal + line_tax` | — | — | — | — | Yes | — |
| `special_instructions` | VARCHAR(280) | N | NULL | Kitchen instruction ("less spicy") | ≤280 plain text | Printed on KOT; no PII expected | — | — | — | Yes | — |

### E16 — ORDER_ITEM_ADDON *(new)* · table `order_item_addons` · immutable

Standard columns: `id`, `tenant_id`, `created_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `order_item_id` | UUID | Y | — | Parent line | same tenant | composite FK | `(tenant_id, order_item_id)` | `(order_item_id, addon_id)` | `(tenant_id, order_item_id)` → `order_items` RESTRICT | Yes | with parent |
| `addon_id` | UUID | Y | — | Source add-on | belongs to line's menu item; available | — | — | as above | `(tenant_id, addon_id)` → `menu_item_addons` RESTRICT | Yes | with parent |
| `name_snapshot` | VARCHAR(60) | Y | — | Add-on name at order time | copied | — | — | — | — | Yes | — |
| `price_snapshot` | NUMERIC(12,2) | Y | — | Add-on price at order time | copied; ≥0 | Authoritative | — | — | — | Yes | — |

### E17 — KOT_TICKET *(baseline, modified)* · table `kot_tickets`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `order_id` | UUID | Y | — | Source order (must be ACCEPTED or later) | same tenant | composite FK | `(tenant_id, order_id)` | U-KOT-2 | `(tenant_id, order_id)` → `orders` RESTRICT | Yes | `kot.generated` |
| `kitchen_section_id` | UUID | N | NULL | Section; NULL = unsectioned kitchen | same tenant | — | `(tenant_id, kitchen_section_id, status)` | U-KOT-2 | `(tenant_id, kitchen_section_id)` → `kitchen_sections` RESTRICT | Yes | `kot.generated` |
| `business_date` | DATE | Y | — | Date for numbering | server-set | — | — | U-KOT-1 | — | Yes | — |
| `kot_number` | VARCHAR(12) | Y | — | `K-NNN` daily sequence | server-generated | — | — | U-KOT-1 | — | Yes | `kot.generated` |
| `round_number` | SMALLINT | Y | `1` | Order round (Q-003) | ≥1 | — | — | U-KOT-2 | — | Yes | `kot.generated` |
| `status` | kot_status | Y | `QUEUED` | QUEUED→PREPARING→READY→SERVED; CANCELLED when order cancelled | state machine BR-KOT-02 | Permission `kot:update_status` / `kot:serve` | `(tenant_id, status, updated_at)` | — | — | Yes | `kot.status_changed` |
| `priority` | order_priority | Y | `NORMAL` | Snapshot of order priority | — | — | — | — | — | Yes | — |
| `order_type_snapshot` | order_type | Y | — | Printed/displayed type | copied | — | — | — | — | Yes | — |
| `table_label_snapshot` | VARCHAR(20) | N | NULL | Printed/displayed table | copied | — | — | — | — | Yes | — |
| `notes_snapshot` | VARCHAR(500) | N | NULL | Order notes | copied | — | — | — | — | Yes | — |
| `queued_at` | TIMESTAMPTZ | Y | `now()` | When the KOT entered the queue (timer start) | server-set | — | — | — | — | Yes | `kot.generated` |
| `preparing_at` / `ready_at` / `served_at` / `cancelled_at` | TIMESTAMPTZ | N | NULL | Transition instants (timers) | server-set | — | — | — | — | Yes | `kot.status_changed` |

U-KOT-1 = `UNIQUE (tenant_id, business_date, kot_number)`.
U-KOT-2 = `UNIQUE NULLS NOT DISTINCT (tenant_id, order_id, kitchen_section_id, round_number)`. This needs PostgreSQL ≥ 15
[assumption — Railway PostgreSQL version confirmed in S1-P02-T001; fallback: unique index on `COALESCE(kitchen_section_id, '00000000-0000-0000-0000-000000000000')`].
It makes KOT generation idempotent (fixes BA-18).

Print state is **derived**: the latest `PRINT_JOB` for the ticket (`api.md` RH-KOT-01 returns `printStatus`).

### E18 — KOT_ITEM *(new)* · table `kot_items` · immutable

Standard columns: `id`, `tenant_id`, `created_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `kot_ticket_id` | UUID | Y | — | Parent KOT | same tenant | composite FK | `(tenant_id, kot_ticket_id)` | `(kot_ticket_id, order_item_id)` | `(tenant_id, kot_ticket_id)` → `kot_tickets` RESTRICT | Yes | with `kot.generated` |
| `order_item_id` | UUID | Y | — | Source line | same order, same section, same round | — | — | as above | `(tenant_id, order_item_id)` → `order_items` RESTRICT | Yes | — |
| `quantity` | SMALLINT | Y | — | Units to prepare | = order line quantity | — | — | — | — | Yes | — |
| `item_label_snapshot` | VARCHAR(190) | Y | — | "Butter Chicken (Full)" | copied | — | — | — | — | Yes | — |
| `addons_snapshot` | VARCHAR(280) | N | NULL | "Extra butter, Jeera rice" | copied | — | — | — | — | Yes | — |
| `instructions_snapshot` | VARCHAR(280) | N | NULL | Line instructions | copied | — | — | — | — | Yes | — |

### E19 — TRANSACTION *(baseline, modified)* · table `transactions` · append-only ledger

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at` (changes only when voided).

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `order_id` | UUID | Y | — | Order paid/refunded | same tenant; not CANCELLED | composite FK | `(tenant_id, order_id)` | — | `(tenant_id, order_id)` → `orders` RESTRICT | Yes | `payment.recorded` / `refund.created` |
| `type` | transaction_type | Y | — | PAYMENT or REFUND | enum | REFUND needs `refund:create` | `(tenant_id, business_date, type)` | — | — | Yes | as above |
| `payment_method` | payment_method | Y | — | CASH / CARD / UPI | enum; REFUND method defaults to original | — | — | — | — | Yes | as above |
| `status` | transaction_status | Y | `SUCCESS` | SUCCESS or VOIDED (recording error) | VOID only same business day, before day close | `transaction:void` | — | — | — | Yes | `transaction.voided` |
| `amount` | NUMERIC(12,2) | Y | — | Amount applied to the order | CHECK > 0; PAYMENT ≤ outstanding balance; REFUND ≤ refundable | Server-validated (BA-20) | — | — | — | Yes | as above |
| `amount_tendered` | NUMERIC(12,2) | N | NULL | Cash handed over | CASH only; ≥ amount | — | — | — | — | Yes | `payment.recorded` |
| `change_due` | NUMERIC(12,2) | N | NULL | tendered − amount | server-computed | — | — | — | — | Yes | `payment.recorded` |
| `reference` | VARCHAR(64) | N | NULL | Card slip / UPI reference | `^[A-Za-z0-9-]{4,64}$`; required for UPI | Never store card numbers (validation rejects 13–19 digit PAN-like strings) | — | — | — | Yes | as above |
| `refund_of_transaction_id` | UUID | N | NULL | Original PAYMENT for a REFUND | required for REFUND; same order | composite FK | — | — | `(tenant_id, refund_of_transaction_id)` → `transactions` RESTRICT | Yes | `refund.created` |
| `reason` | VARCHAR(280) | N | NULL | Refund reason | required for REFUND (5–280) | — | — | — | — | Yes | `refund.created` |
| `business_date` | DATE | Y | — | Restaurant-local date recorded | server-set | Closed days reject writes | `(tenant_id, business_date, type)` | — | — | Yes | — |
| `idempotency_key` | UUID | Y | — | Replay protection | UUID | — | — | `(tenant_id, idempotency_key)` | — | Yes | — |
| `recorded_by_user_id` | UUID | Y | — | Staff who recorded | server-set | — | `(tenant_id, recorded_by_user_id, created_at)` | — | `users.id` RESTRICT | Yes | as above |
| `voided_at` | TIMESTAMPTZ | N | NULL | Void instant | server-set | — | — | — | — | Yes | `transaction.voided` |
| `voided_by_user_id` | UUID | N | NULL | Who voided | server-set | — | — | — | `users.id` RESTRICT | Yes | `transaction.voided` |
| `void_reason` | VARCHAR(280) | N | NULL | Why voided | required (5–280) when voiding | — | — | — | — | Yes | `transaction.voided` |

### E20 — BUSINESS_DAY_CLOSE *(new)* · table `business_day_closes` · reconciliation (brief §31)

Standard columns: `id`, `tenant_id`, `created_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `business_date` | DATE | Y | — | Day being closed | ≤ today (restaurant tz); not already closed | — | — | `(tenant_id, business_date)` | — | Yes | `day_close.performed` |
| `expected_cash` | NUMERIC(12,2) | Y | — | Σ CASH payments − Σ CASH refunds (SUCCESS) | server-computed | — | — | — | — | Yes | as above |
| `counted_cash` | NUMERIC(12,2) | Y | — | Cash counted by staff | ≥0 | — | — | — | — | Yes | as above |
| `cash_variance` | NUMERIC(12,2) | Y | — | counted − expected | server-computed | — | — | — | — | Yes | as above |
| `card_total` | NUMERIC(12,2) | Y | — | Net CARD | server-computed | — | — | — | — | Yes | as above |
| `upi_total` | NUMERIC(12,2) | Y | — | Net UPI | server-computed | — | — | — | — | Yes | as above |
| `refund_total` | NUMERIC(12,2) | Y | — | All refunds | server-computed | — | — | — | — | Yes | as above |
| `order_count` | INTEGER | Y | — | Orders with that business date (non-cancelled) | server-computed | — | — | — | — | Yes | as above |
| `open_order_count` | INTEGER | Y | — | Orders still not COMPLETED/CANCELLED/REFUNDED | server-computed; shown as warning | — | — | — | — | Yes | as above |
| `notes` | VARCHAR(500) | N | NULL | Variance explanation | required if variance ≠ 0 | — | — | — | — | Yes | as above |
| `closed_by_user_id` | UUID | Y | — | Closer | server-set | — | — | — | `users.id` RESTRICT | Yes | as above |

### E21 — PRINTER *(new)* · table `printers`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `print_agent_id` | UUID | N | NULL | Agent that drives this printer | same tenant; ACTIVE agent | composite FK | `(tenant_id, print_agent_id)` | — | `(tenant_id, print_agent_id)` → `print_agents` RESTRICT | Yes | `printer.updated` |
| `kitchen_section_id` | UUID | N | NULL | KOTs of this section route here; NULL = all sections | same tenant | — | — | — | `(tenant_id, kitchen_section_id)` → `kitchen_sections` RESTRICT | Yes | `printer.updated` |
| `name` | VARCHAR(60) | Y | — | "Kitchen Tandoor 80mm" | trim, 2–60 | — | — | `(tenant_id, lower(name))` | — | Yes | `printer.created/updated` |
| `purpose` | printer_purpose | Y | — | What it prints | enum | — | — | — | — | Yes | as above |
| `connection_type` | printer_connection | Y | — | USB or LAN | enum | — | — | — | — | Yes | as above |
| `connection_address` | VARCHAR(255) | Y | — | LAN: `host:port` (private IPv4, port 1–65535, default 9100); USB: OS device path/name reported by agent | per type; LAN host must be RFC 1918 or link-local | **Used only by the local agent; the cloud server never connects to it** (SSRF — SC-PRINT-06) | — | — | — | Yes | as above |
| `paper_width_mm` | SMALLINT | Y | `80` | Paper width | 58 or 80 | — | — | — | — | Yes | as above |
| `is_active` | BOOLEAN | Y | `true` | Accepts new jobs | — | — | — | — | — | Yes | `printer.deactivated` |
| `health` | printer_health | Y | `UNKNOWN` | Last agent-reported status | enum | — | — | — | — | Yes | — (high-frequency; logged, not audited) |
| `health_reported_at` | TIMESTAMPTZ | N | NULL | When reported | server-set | — | — | — | — | Yes | — |

### E22 — PRINT_AGENT *(new)* · table `print_agents`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `name` | VARCHAR(60) | Y | — | "Counter PC" | trim, 2–60 | — | — | `(tenant_id, lower(name))` | — | Yes | `print_agent.created` |
| `status` | print_agent_status | Y | `PENDING_PAIRING` | Lifecycle | enum | Only ACTIVE authenticates | `(tenant_id, status)` | — | — | Yes | `print_agent.paired`, `print_agent.revoked` |
| `pairing_code_hash` | CHAR(64) | N | NULL | SHA-256 of one-time pairing code | cleared on use/expiry | Code shown once; never stored plain | — | Y | — | Yes | — |
| `pairing_expires_at` | TIMESTAMPTZ | N | NULL | Code expiry (created + 10 min) | — | — | — | — | — | Yes | — |
| `token_hash` | CHAR(64) | N | NULL | SHA-256 of bearer token | set at pairing | Never logged/returned after pairing | — | Y | — | Yes | — |
| `token_prefix` | CHAR(8) | N | NULL | First 8 chars for identification in UI | — | Not sufficient to authenticate | — | — | — | Yes | `print_agent.paired` |
| `agent_version` | VARCHAR(32) | N | NULL | Reported version | semver | — | — | — | — | Yes | — |
| `os_info` | VARCHAR(64) | N | NULL | Reported OS | ≤64 | — | — | — | — | Yes | — |
| `last_seen_at` | TIMESTAMPTZ | N | NULL | Last authenticated call | server-set (throttled 1 write/10 s) | Offline if > 90 s | — | — | — | Yes | — |
| `last_seen_ip` | INET | N | NULL | Source IP of last call | server-set | Security investigation only; TENANT_ADMIN visible | — | — | — | Yes | — |
| `created_by_user_id` | UUID | Y | — | Creator | server-set | — | — | — | `users.id` RESTRICT | Yes | `print_agent.created` |
| `paired_at` | TIMESTAMPTZ | N | NULL | Pairing instant | — | — | — | — | — | Yes | `print_agent.paired` |
| `revoked_at` | TIMESTAMPTZ | N | NULL | Revocation instant | — | — | — | — | — | Yes | `print_agent.revoked` |
| `revoked_by_user_id` | UUID | N | NULL | Revoker | — | — | — | — | `users.id` RESTRICT | Yes | `print_agent.revoked` |

### E23 — PRINT_JOB *(baseline, modified)* · table `print_jobs`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `printer_id` | UUID | Y | — | Target printer | same tenant; active | composite FK | IX-PJ-1 | — | `(tenant_id, printer_id)` → `printers` RESTRICT | Yes | `print_job.created` |
| `print_agent_id` | UUID | N | NULL | Agent holding/last holding the lease | server-set at claim | Ack accepted only from this agent | — | — | `(tenant_id, print_agent_id)` → `print_agents` RESTRICT | Yes | — |
| `job_type` | print_job_type | Y | — | KOT / RECEIPT / TEST | enum | — | — | — | — | Yes | `print_job.created` |
| `order_id` | UUID | N | NULL | Related order | same tenant | — | `(tenant_id, order_id)` | — | `(tenant_id, order_id)` → `orders` RESTRICT | Yes | — |
| `kot_ticket_id` | UUID | N | NULL | Related KOT | same tenant; required when `job_type = KOT` | — | `(tenant_id, kot_ticket_id)` | — | `(tenant_id, kot_ticket_id)` → `kot_tickets` RESTRICT | Yes | — |
| `dedupe_key` | VARCHAR(120) | Y | — | Logical job identity (`KOT:{id}:v1`, `RECEIPT:{orderId}:v{n}`, `TEST:{uuid}`) | server-built | Duplicate-prevention (T-008) | — | `(tenant_id, dedupe_key)` | — | Yes | — |
| `is_reprint` | BOOLEAN | Y | `false` | Explicit reprint | — | — | — | — | — | Yes | `print_job.reprint_requested` |
| `payload` | JSONB | Y | — | Printer-agnostic render document (`architecture.md` §6.4) | Zod `PrintDocument` schema; ≤64 KB | Contains only printed content; no tokens, no customer phone/email | — | — | — | Yes | — |
| `status` | print_job_status | Y | `PENDING` | PENDING→PROCESSING→PRINTED / FAILED | ADR-007 | PRINTED only via agent ack | IX-PJ-1 | — | — | Yes | `print_job.failed` (terminal), `print_job.retried` |
| `attempt_count` | SMALLINT | Y | `0` | Claims so far | 0–max | — | — | — | — | Yes | — |
| `max_attempts` | SMALLINT | Y | `3` | Automatic retry ceiling | 1–10 | — | — | — | — | Yes | — |
| `next_attempt_at` | TIMESTAMPTZ | Y | `now()` | Earliest claim time (backoff) | server-set | — | IX-PJ-1 | — | — | Yes | — |
| `claim_token` | UUID | N | NULL | Current lease token | server-set per claim | Stale acks rejected (409) | — | — | — | Yes | — |
| `claimed_at` | TIMESTAMPTZ | N | NULL | Last claim | — | — | — | — | — | Yes | — |
| `lease_expires_at` | TIMESTAMPTZ | N | NULL | claimed_at + 60 s | — | — | — | — | — | Yes | — |
| `printed_at` | TIMESTAMPTZ | N | NULL | Agent-confirmed print | — | — | — | — | — | Yes | — |
| `failed_at` | TIMESTAMPTZ | N | NULL | Terminal failure instant | — | — | — | — | — | Yes | `print_job.failed` |
| `last_error_code` | VARCHAR(40) | N | NULL | `PRINTER_OFFLINE`, `PAPER_OUT`, `ENCODING_ERROR`, `TIMEOUT`, `UNKNOWN` | enum-like whitelist | — | — | — | — | Yes | — |
| `last_error_message` | VARCHAR(500) | N | NULL | Agent-supplied detail | truncated 500; control chars stripped | Treated as untrusted text (escaped in UI) | — | — | — | Yes | — |
| `requested_by_user_id` | UUID | N | NULL | Staff who triggered (NULL = automatic) | server-set | — | — | — | `users.id` RESTRICT | Yes | — |

IX-PJ-1 = `(tenant_id, printer_id, status, next_attempt_at)`; also `(tenant_id, updated_at)`.

### E24 — AUDIT_LOG *(baseline, modified)* · table `audit_logs` · append-only

Standard columns: `id`, `created_at`. `tenant_id` is **nullable**: NULL means a platform-level event.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | N | NULL | Tenant the event concerns; NULL = platform | server-set | Tenant users see only their tenant's rows; SUPER_ADMIN sees platform rows and tenant lifecycle rows | `(tenant_id, created_at DESC)` | — | `tenants.id` RESTRICT | Yes (nullable) | n/a |
| `actor_type` | audit_actor_type | Y | — | Who acted | enum | — | — | — | — | — | — |
| `actor_user_id` | UUID | N | NULL | Acting user | server-set | — | `(actor_user_id, created_at)` | — | `users.id` RESTRICT | — | — |
| `actor_agent_id` | UUID | N | NULL | Acting print agent | server-set | — | — | — | — (no FK; keeps log independent) | — | — |
| `actor_role` | VARCHAR(20) | N | NULL | Role at time of action (`SUPER_ADMIN`, `MANAGER`…) | server-set | — | — | — | — | — | — |
| `action` | VARCHAR(80) | Y | — | `domain.verb` (catalogue in `security.md` §7) | must be in `AUDIT_ACTIONS` const | — | `(tenant_id, action, created_at)` | — | — | — | — |
| `resource_type` | VARCHAR(40) | Y | — | Entity name (`ORDER`) | whitelist | — | `(tenant_id, resource_type, resource_id)` | — | — | — | — |
| `resource_id` | UUID | N | NULL | Entity id | — | — | as above | — | — | — | — |
| `before_state` | JSONB | N | NULL | Changed fields before | redacted by `lib/audit/redact.ts`; ≤16 KB | No secrets, tokens, OTPs; PII masked | — | — | — | — | — |
| `after_state` | JSONB | N | NULL | Changed fields after | as above | as above | — | — | — | — | — |
| `reason` | VARCHAR(500) | N | NULL | User-supplied reason (cancel/refund/void/suspend) | ≤500 | — | — | — | — | — | — |
| `request_id` | VARCHAR(64) | N | NULL | Correlation id (`x-request-id`) | server-set | — | — | — | — | — | — |
| `ip_address` | INET | N | NULL | Client IP (from trusted proxy header) | server-set | Personal data; retention Q-024 | — | — | — | — | — |
| `user_agent` | VARCHAR(256) | N | NULL | Truncated UA | ≤256 | — | — | — | — | — | — |

Immutability: migration creates `trigger audit_logs_no_update_delete` (`BEFORE UPDATE OR DELETE … RAISE EXCEPTION`) and the statement-level `audit_logs_no_truncate` (`BEFORE TRUNCATE`). Writes happen inside the same DB
transaction as the audited change (SC-AUD-02).

### E25 — SOCIAL_POST *(baseline, modified)* · table `social_posts`

Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `channel` | social_channel | Y | — | Intended destination (manual) | enum | — | — (list index is `(tenant_id, status, created_at)`) | — | — | Yes | `social_post.created` |
| `card_type` | social_card_type | Y | — | Which menu card to generate | enum | Card shows only public data | — | — | — | Yes | as above |
| `daily_menu_id` | UUID | N | NULL | Source daily menu | required if card_type = DAILY_MENU; must be PUBLISHED | — | — | — | `(tenant_id, daily_menu_id)` → `daily_menus` RESTRICT | Yes | as above |
| `menu_item_id` | UUID | N | NULL | Source item | required if card_type = MENU_ITEM; published | — | — | — | `(tenant_id, menu_item_id)` → `menu_items` RESTRICT | Yes | as above |
| `caption` | VARCHAR(2200) | Y | — | Post text | 1–2200 plain text | — | — | — | — | Yes | `social_post.updated` |
| `share_url` | VARCHAR(2048) | Y | — | Server-built public URL (`/r/{slug}` or share page) | server-built only | Never user-supplied | — | — | — | Yes | — |
| `status` | social_post_status | Y | `DRAFT` | DRAFT→READY→MARKED_POSTED; any→ARCHIVED | state machine BR-SOC-02 | UI wording "Marked as posted by {user}", never "Published successfully" | as above | — | — | Yes | `social_post.marked_posted` |
| `posted_url` | VARCHAR(2048) | N | NULL | Link to the real post, entered by staff | `https:` only | Rendered as plain link with `rel="noopener noreferrer nofollow"` | — | — | — | Yes | `social_post.marked_posted` |
| `marked_posted_at` | TIMESTAMPTZ | N | NULL | When marked | server-set | — | — | — | — | Yes | as above |
| `marked_posted_by_user_id` | UUID | N | NULL | Who attested | server-set | — | — | — | `users.id` RESTRICT | Yes | as above |
| `created_by_user_id` | UUID | Y | — | Author | server-set | — | — | — | `users.id` RESTRICT | Yes | `social_post.created` |

### E26 — TENANT_COUNTER *(new)* · table `tenant_counters`

No `id`. PK `(tenant_id, counter_type, business_date)`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_id` | UUID | Y | — | Tenant | — | server-derived | PK | PK | `tenants.id` RESTRICT | Yes | — |
| `counter_type` | counter_type | Y | — | ORDER / KOT | enum | — | PK | PK | — | Yes | — |
| `business_date` | DATE | Y | — | Day | — | — | PK | PK | — | Yes | — |
| `last_value` | INTEGER | Y | `0` | Last issued number | ≥0; ≤9999 (order) / ≤999 (KOT) — overflow raises error | — | — | — | — | Yes | — |
| `updated_at` | TIMESTAMPTZ | Y | `now()` | Last increment | — | — | — | — | — | Yes | — |

### E27 — RATE_LIMIT_BUCKET *(new)* · table `rate_limit_buckets` · not tenant-owned

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `bucket_key` | VARCHAR(128) | Y | — | `scope:sha256(identifier)` | server-built | No raw IP/token stored | PK | PK | — | No | — |
| `window_start` | TIMESTAMPTZ | Y | — | Current window start | — | — | — | — | — | No | — |
| `hit_count` | INTEGER | Y | `0` | Hits in window | ≥0 | — | — | — | — | No | — |
| `expires_at` | TIMESTAMPTZ | Y | — | Deletion eligibility | — | — | `(expires_at)` | — | — | No | — |

### E28 — MEDIA_ASSET *(new, decision-gated by Q-009)* · table `media_assets`

Created only if Q-009 approves uploads. Otherwise image fields accept allow-listed HTTPS URLs only.
Standard columns: `id`, `tenant_id`, `created_at`, `updated_at`.

| Field | Type | Req | Default | Meaning | Validation | Security | Idx | Uniq | FK | Tenant scoped | Audit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `purpose` | media_purpose | Y | — | LOGO / COVER / MENU_ITEM | enum | — | `(tenant_id, purpose)` | — | — | Yes | `media.uploaded` |
| `storage_key` | VARCHAR(255) | Y | — | `tenants/{tenantId}/{uuid}.{ext}` | server-built | Key prefix enforces tenant separation in bucket | — | Y | — | Yes | — |
| `content_type` | VARCHAR(40) | Y | — | `image/jpeg`, `image/png`, `image/webp` | magic-byte sniffed, not client-declared | SVG rejected (XSS) | — | — | — | Yes | — |
| `byte_size` | INTEGER | Y | — | Size | ≤ 5 MB | — | — | — | — | Yes | — |
| `width` / `height` | INTEGER | Y | — | Pixels after re-encode | ≤ 4096 | Re-encoded to strip EXIF/GPS | — | — | — | Yes | — |
| `sha256` | CHAR(64) | Y | — | Content hash | — | — | — | — | — | Yes | — |
| `status` | media_status | Y | `PENDING_UPLOAD` | Lifecycle | enum | Only READY URLs usable | — | — | — | Yes | `media.deleted` |
| `uploaded_by_user_id` | UUID | Y | — | Uploader | server-set | — | — | — | `users.id` RESTRICT | Yes | `media.uploaded` |

---

## 3. Cross-entity invariants

| ID | Invariant | Enforced by |
|---|---|---|
| INV-01 | Every tenant-owned row's `tenant_id` equals its parent's `tenant_id` | Composite FKs (ADR-008) |
| INV-02 | `orders.total_amount = subtotal_amount + tax_amount − discount_amount` and each line's `line_total = line_subtotal + line_tax` | CHECK constraints + `lib/pricing` unit tests |
| INV-03 | `orders.subtotal_amount = Σ order_items.line_subtotal` (same for tax) | Computed in one transaction; integration test TC-ORDER-004 |
| INV-04 | `orders.paid_amount = Σ SUCCESS PAYMENT`, `refunded_amount = Σ SUCCESS REFUND`, `refunded_amount ≤ paid_amount` | Recomputed in the same transaction as any ledger write; CHECK |
| INV-05 | A KOT exists only for orders with status ∈ {ACCEPTED, PREPARING, READY, COMPLETED, CANCELLED} | Service guard + TC-KOT-001 |
| INV-06 | `print_jobs.status = PRINTED` ⇒ `printed_at IS NOT NULL` and set by agent ack | CHECK + no other code path (TC-PRINT-006) |
| INV-07 | A tenant with ≥1 activated membership has ≥1 ACTIVE TENANT_ADMIN | Service guard + TC-RBAC-012 |
| INV-08 | `audit_logs` rows are never updated or deleted | DB trigger + TC-AUDIT-003 |
| INV-09 | `restaurants.currency_code` cannot change once any order exists | Service guard + TC-REST-006 |
| INV-10 | No column anywhere stores a subscription plan, tier or recurring billing state | Schema review in S1-P02-T002 + consistency scan (S1-P28-T004) |

### 3.1 Additional database constraints

`0001_init` also enforces these defensive CHECKs, which are stricter than the dictionary rows above and never contradict them
(catalogue test TC-DB-005 has a negative case for every CHECK in the database): `orders` paid/refunded/version ≥ 0 and
`currency_code ~ '^[A-Z]{3}`; `order_items` add-ons total and line tax ≥ 0; `kot_items.quantity` 1–99; `transactions`
`change_due ≥ 0`, PAYMENT rows have no `refund_of_transaction_id`, SUCCESS rows have no `voided_at`; `business_day_closes`
`cash_variance = counted_cash − expected_cash`, refund total and counts ≥ 0.
