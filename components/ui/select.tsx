/**
 * Moved to the form system (S1-P08-T005). Kept as a re-export so existing imports keep working.
 * @deprecated import from "@/components/ui/inputs".
 */
export { Select, SelectInput, type SelectOption, type SelectProps } from "./inputs/select";
